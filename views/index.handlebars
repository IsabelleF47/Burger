<h1>Burger Barn Baby!</h1>

<h2>Create and eat delicious burgers!</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{burgers/burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>The variety is endless!</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{burgers/burger-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Create a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">Eat da burger</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten<br>
    <input type="radio" name="eaten" value="0"> Still avaliable for da eating
  </div>

  <button type="submit">Add Burger</button>
</form>
